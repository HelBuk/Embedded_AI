\documentclass[12pt,a4paper]{report}

% ======= Packages =======
\usepackage[margin=2.5cm]{geometry}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage[hidelinks]{hyperref}
\usepackage{tocloft}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{caption}
\usepackage{float}
\usepackage{fancyhdr}
\usepackage{amsmath}
\usepackage{subcaption}
\usepackage{url}
\usepackage{csquotes}
\usepackage{tabularx}

\newcommand{\TP}{\mathrm{TP}}
\newcommand{\FP}{\mathrm{FP}}
\newcommand{\FN}{\mathrm{FN}}
\newcommand{\IoU}{\mathrm{IoU}}
\newcommand{\AP}{\mathrm{AP}}
\newcommand{\mAP}{\mathrm{mAP}}
\newcommand{\Precision}{\mathrm{P}}
\newcommand{\Recall}{\mathrm{R}}

% ======= Basic Formatting =======
\onehalfspacing
\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}

% TOC spacing a bit tighter
\setlength{\cftbeforechapskip}{6pt}
\setlength{\cftbeforesecskip}{2pt}

% Chapter title style (compact and clean)
\titleformat{\chapter}[block]{\Huge\bfseries}{\thechapter}{1em}{}
\titlespacing*{\chapter}{0pt}{-6pt}{12pt}

% Header/Footer
\pagestyle{fancy}
\fancyhf{}
\lhead{\leftmark}
\rfoot{\thepage}

% ======= Title Page Variables (edit these) =======
\newcommand{\ReportTitle}{Embedded AI}
\newcommand{\AuthorName}{Bukueva Elena}
\newcommand{\SupervisorName}{Geir Mathisen}

\newcommand{\DepartmentName}{Faculty of Information Technology and Electrical Engineering}
\newcommand{\InstitutionName}{Norwegian University of Science and Technology (NTNU)}
\newcommand{\LocationDate}{Trondheim, \today}
\newcommand{\ThesisType}{Project}
\newcommand{\ReportSubtitle}{}

% ======= Document =======
\begin{document}


\hypersetup{pageanchor=false} % avoid duplicate page anchors in front matter


% ======= Front Page (mirrors “NTNU’s standard front page” placeholder) =======
\begin{titlepage}
    \centering
    \vspace*{1cm}
    {\Large \InstitutionName\par}
    \vspace{1.0cm}
    {\large \DepartmentName\par}
    \vfill
    {\LARGE \textbf{\ReportTitle}\par}
    \vspace{0.3cm}
    {\Large \ReportSubtitle\par}
    \vspace{1.2cm}
    {\Large \textbf{\ThesisType}\par}
    \vspace{1.2cm}
    \begin{tabular}{@{}ll}
        Author:      & \AuthorName \\
        Supervisor:  & \SupervisorName \\
        Date:        & \LocationDate \\
    \end{tabular}
    \vfill
    \includegraphics[width=0.25\textwidth]{ntnu_bredde_eng.png}\\[0.5cm]
\end{titlepage}
\cleardoublepage

% ======= Task Description (Oppgavebeskrivelse) =======
\chapter*{Task Description}
\addcontentsline{toc}{chapter}{Task Description}

\section*{Goal}

    Explore Edge AI constrains and deployment tools through the
    task of Real-Time Fasteners Detection on Raspberry Pi 4\&5.
    The system must run on-device, produce stable detections
    suitable for downstream sorting and meet latency, memory and
    environment constraints typical for edge environments.

\section*{Operating Context \& Constraints}

    \begin{itemize}
        \item Hardware: Raspberry Pi 4 (4 GB), Raspberry Pi 5 (8 GB) and Hailo-8 (26 TOPS) accelerator.
        \item Camera: UI-3590CP-C-HQ Rev.2 - IDS uEye running up to 21 FPS with configurable exposure(e.g., 100 000 µs)
        \item Environment: Conveyor Belt with chosen speed during inference -  2 cm/s; occasional vibration and lighting drift.
    \end{itemize}

\section*{Method}

    \begin{itemize}
        \item \textbf{Modeling}
            \begin{itemize}
                \item Defining the set of suitable for Edge AI models;
                \item Exploration theirs constraints and performance;
            \end{itemize}
        \item \textbf{Annotation \& Dataset}
            \begin{itemize}
                \item Camera SDK setup and programming;
                \item Define annotation format and tools;
                \item Perform annotation and prepare suitable for the chosen model
                    dataset;
             \end{itemize}
        \item \textbf{Training}
            \begin{itemize}
                \item Perform a Transfer Learning Training on a GPU;
                \item Defining the pretrained weights based on the task
                        and required annotation quality;
            \end{itemize}
        \item \textbf{Quantization \& Compilation}
            \begin{itemize}
                \item Configuration of a Hailo Compiler for the Hailo-8 AI
                    Accelerator deployment;
                \item INT8 PTQ with calibration set; Export .onnx → .har → .hef;
            \end{itemize}
        \item \textbf{Edge Pipeline}
            \begin{itemize}
                    \item Programming of the Camera Real-Time Streaming;
                    \item Perform Real-Time Inference;
                \item
            \end{itemize}
        \item \textbf{Benchmarking}
            \begin{itemize}
                \item CPU vs Hailo-8 AI comparison;
                \item FPS and Detection Metrics measures;
                \item Model Performance Comparison;
            \end{itemize}
    \end{itemize}

    \section*{Deliverables}
    \begin{itemize}
        \item Annotated Dataset
        \item Trained Weights (FP32 + INT8) and compiled \texttt{.hef} for YOLOv8 \& YOLO11 (-nano and -small)
        \item Benchmark Report (tables, plots of accuracy, latency, FPS)
        \item Deployment Guide (Raspberry Pi setup, uEye drivers installation guide, Hailo API configurations,
            Calibration pack)
    \end{itemize}

\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}
This project investigates practical Edge-AI deployment for the Real-Time fastener detection on
Raspberry Pi 4/5, with and without Hailo-8 AI accelerator. It targets a detection on a
conveyer belt with motion \( \approx 2 cm/s\), using an IDS uEye camera with maximum 21 FPS capability.
A lightweight YOLO variants (v8n/v8s/v11n/v11s) are trained via transfer learning on a domain-specific
fastener dataset. Four classes of objects are used during the training: machine screws, wood screws, nuts
and washers. An additional fifth class is used to identify incomplete parts of the object, when neither
a human, not a model can correctly classify an object. The dataset is prepared with the help of Roboflow framework, and it consists of 2131 augmented
960x960 grayscale images. Models are exported to ONNX format and compiled with the Hailo toolchain
(ONNX \(\to\) HAR \(\to\) HEF) using INT8 post-training quantization from a calibration set.  \\
A discussion of the annotation format choices, compilation workflow and on-device inference constraints is
provided in the literature overview section. \\
Evaluation performed based on standard detection metrics
(IoU, AP, mAP@0.50 and mAPP@[0.5:0.95]) and runtime
(latency/FPS). On GPU, all tested YOLO versions archived competitive accuracy on the gathered dataset.
While on Raspberry PI 4/5 CPU, real-time throughput is not met, Hailo accelerator shows significantly
higher raw inference capacity, having hardware benchmarks \( \approx 142 FPS\), while Pi 5 pipeline
delivers \( \approx 1 FPS\). However the archieved inference accuracy after
the performed quantization is visibly lower than the pure YOLO PyTorch checkpoint inference.
The studies on image size and NMS settings, and briefly analyzed quantization effects on small,
fine-grained targets are reported.


\cleardoublepage
% ======= Table of Contents =======
\tableofcontents
\cleardoublepage

% ======= Abbreviations (Brukte forkortelser) =======
\chapter*{Abbreviations}
\addcontentsline{toc}{chapter}{Abbreviations}
\begin{longtable}{@{}p{3cm}p{10cm}@{}}
AI & Artificial Intelligence \\
AABB & Axis-Aligned Bounding Box \\
AP & Average Precision \\
API & Application Programming Interface \\
ARM & Arm architecture (CPU family) \\
CCW & Counterclockwise \\
COCO & Common Objects in Context (dataset) \\
CPU & Central Processing Unit \\
DL & Deep Learning \\
ES (in OpenGL ES) & OpenGL for Embedded Systems \\
FN & False Negative \\
FP & False Positive \\
FP32 & 32-bit floating point \\
FLOPs & Floating-Point Operations \\
FPS & Frames Per Second \\
GFLOPs & Giga Floating-Point Operations \\
GPU & Graphics Processing Unit \\
HAR & Hailo Archive (intermediate model package) \\
HEF & Hailo Executable Format (compiled binary for Hailo device) \\
HN & Hailo Network (graph JSON inside HAR) \\
IDS & Imaging Development Systems (uEye camera vendor) \\
INT8 & 8-bit integer \\
IoU & Intersection over Union \\
L2/L3 & Level-2 / Level-3 CPU cache \\
mAP & mean Average Precision \\
NMS & Non-Maximum Suppression \\
NPZ & NumPy Zip archive (weights/tensors) \\
OBB & Oriented (Rotated) Bounding Box \\
ONNX & Open Neural Network Exchange \\
OpenGL ES & OpenGL for Embedded Systems \\
PTQ & Post-Training Quantization \\
RAM & Random Access Memory \\
SDK & Software Development Kit \\
SoC & System on Chip \\
TOPS & Tera Operations Per Second \\
TP & True Positive \\
uEye & IDS camera/product line \\
VStream & (Hailo) Virtual Stream (I/O stream abstraction) \\
Vulkan & Low-level graphics/compute API \\

\end{longtable}
\cleardoublepage
\cleardoublepage
\hypersetup{pageanchor=true}

% ======= 1 Introduction (Innledning) =======
\chapter{Introduction}

\section{Background of the Assignment}
% Need, prior solutions, how this project fits into the bigger picture, what it builds upon.
\section{Motivation}
% Why this work is done.
\section{Limitations}
% Scope limitations; resource constraints and non-ideal choices.
\section{Contributions}
% Concise list of the novel/smart parts of the work.
\section{Thesis Structure}
% 1–3 sentences about what each chapter contains.

% ======= 2 Literature Study (Litteraturstudie) =======
\chapter{Literature Study}
\noindent

\section{Datasets and Annonation strategies for Detection Tasks}
    Object detection models localize objects on an input picture or on a set of video frames.
    Localization can be performed by different approaches, the most relative to this work are:
    \begin{itemize}
        \item Axis-aligned bounding  box (AABB): (x, y, w, h), which is cheap to annotate
            and fast to predict.
        \item Oriented/rotated bounding box (OBB): apart from AABB an angle $\theta$ of
            the box is introduced,  which is a bit costlier to label.
        \item Polygon: a list of vertices tracing the object boundary. Provides precise
            geometry, however is time-consuming to label.
        \item Pixel mask (\enquote{painted area}) - most precise 2D shape, ideal for measurement
            and robotics, heaviest to annotate and train on.
    \end{itemize}

    Choosing the right localization strategy should be guided by the structure of
    the available pretrained weights. Training large detectors from scratch is
    both resource- and time-intensive. For example, Ultralytics estimates that
    training YOLOv5 on COCO (118k images, 300 epochs) on a single
    NVIDIA V100-16GB takes approximately $n/s/m/l/x \approx 1/2/4/6/8$ days,
    respectively \cite{UltralyticsYOLOv5Quickstart}. Collecting and annotating
    a dataset of that scale is even more time-consuming. Consequently,
    it is preferable to start from weights pretrained on a dataset whose
    \emph{labeling granularity} matches the task (e.g., axis-aligned boxes
    vs.\ oriented boxes vs.\ masks). If the required annotation is more
    detailed than the pretraining labels, an additional annotation
    effort is needed for transfer learning. Then, if axis-aligned bounding
    boxes (AABB) are too coarse for small or complex objects, a
    richer localization (e.g., oriented boxes or instance masks) to meet
    accuracy requirements would be a better choice.

    Mentioned earlier COCO dataset \cite{Lin2014COCO} is the common
    detection benchmark. It is big enough, varied, well-annotated and
    permissively licensed. The dataset contains photos of 91 objects
    types with a total of 2.5 million labeled instances in 328k
    images.

    COCO’s \cite{Lin2014COCO} official detection annotations are
    axis-aligned boxes only (bbox: [x, y, width, height]). There
    are no oriented boxes in COCO, so the standard YOLOv8/YOLO11
    \cite{Redmon2016YOLO} \textit{detect} checkpoints (trained
    on COCO 2017) learn AABB by default:

    \begin{verbatim}
    {
      "id": 1768,
      "image_id": 397133,
      "category_id": 1,
      "bbox": [73.0, 43.0, 199.0, 305.0],
      "area": 39220.0,
      "iscrowd": 0,
      "segmentation": [
        [78.0, 45.0, 86.0, 45.0, 95.0, 47.0, 101.0, 51.0, ...]
      ]
    }
    \end{verbatim}


    \begin{figure}[H]
      \centering
      \includegraphics[width=0.3\linewidth]{pics/COCO_val2017_285.pdf}
      \caption{Example picture from the COCO dataset with AABB annotation \cite{COCO_image_285}}
    \end{figure}

    For oriented bounding boxes (OBB), the DOTA dataset
    \cite{Xia2018DOTA} is the pioneering benchmark.
    Ultralytics provides YOLO \texttt{*-obb} weights pretrained
    on DOTA \cite{UltralyticsYOLO11Repo}.

    Other common datasets are:

    \begin{itemize}
        \item Objects365 \cite{shao2019objects365} (~600k images, 365 classes):
            broader coverage than COCO;
            popular for pretraining backbones/heads before COCO fine-tuning.
        \item Open Images \cite{kuznetsova2020open} (millions of images; boxes,
            masks, relationships):
            huge scale, but label noise is higher; great for scale, weaker for
            cleanliness.
    \end{itemize}

    None of these datasets, however, include \enquote{fasteners} as a class.
    In order to particularly train model on fasteners, a MVTec screws
    dataset \cite{ulrich2019comparison} might be used.
    This dataset has oriented-box annotations for screws/nuts
    (384 images, 4,426 OBBs). However, the labeling.

    These are the 13 categories used in the MVTec Screws oriented-box dataset
    (category\_id $\to$ name) :

    1 $\to$ long lag screw \\
    2 $\to$ lag wood screw \\
    3 $\to$ wood screw \\
    4 $\to$ short wood screw \\
    5 $\to$ shiny screw \\
    6 $\to$ black oxide screw \\
    7 $\to$ nut \\
    8 $\to$ bolt \\
    9 $\to$ large nut \\
    10 $\to$ nut \\
    11 $\to$ nut \\
    12 $\to$ machine screw \\
    13 $\to$ short machine screw \\

    \begin{figure}[H]
      \centering
      \includegraphics[width=0.5\linewidth]{pics/screws_dataset.png}
      \caption{Example picture from the MVTec Screws dataset with OBB annotation \cite{MVTec_Screws_2020}}
    \end{figure}

    It is also possible to convert OBB annotation to AABB annotation with some
    loss of information. In order to do so:

    Let $w,h$ be side lengths and $\theta$ the rotation (radians, CCW). The tight
    axis-aligned box around the rotated rectangle has
    \begin{align}
    W_{\text{AABB}} &= \lvert\cos\theta\rvert\, w + \lvert\sin\theta\rvert\, h, \\
    H_{\text{AABB}} &= \lvert\sin\theta\rvert\, w + \lvert\cos\theta\rvert\, h .
    \end{align}
    With the same center $(c_x,c_y)$,
    \begin{align}
    x_{\min} &= c_x - \frac{W_{\text{AABB}}}{2}, &
    x_{\max} &= c_x + \frac{W_{\text{AABB}}}{2}, \\
    y_{\min} &= c_y - \frac{H_{\text{AABB}}}{2}, &
    y_{\max} &= c_y + \frac{H_{\text{AABB}}}{2}.
    \end{align}

    If the oriented box is given by four corners $(x_i,y_i)$, the enclosing AABB is
    \begin{align}
    x_{\min}=\min_i x_i,\quad x_{\max}=\max_i x_i,\qquad
    y_{\min}=\min_i y_i,\quad y_{\max}=\max_i y_i .
    \end{align}

    \textbf{COCO bbox} uses $[x_{\min},\,y_{\min},\,\text{width},\,\text{height}]$, then: \\
    \begin{align}
    \text{bbox}_{\text{COCO}}
    = \bigl[\, x_{\min},\; y_{\min},\; x_{\max}-x_{\min},\; y_{\max}-y_{\min} \,\bigr].
    \end{align}

    \textbf{YOLO txt} uses normalized $(x_\text{center},y_\text{center},w,h)$, then: \\
    Given image size $(W_{\text{img}},H_{\text{img}})$,
    \begin{align}
    x_\text{center} &= \frac{x_{\min}+x_{\max}}{2\,W_{\text{img}}}, &
    y_\text{center} &= \frac{y_{\min}+y_{\max}}{2\,H_{\text{img}}}, \\
    w               &= \frac{x_{\max}-x_{\min}}{W_{\text{img}}}, &
    h               &= \frac{y_{\max}-y_{\min}}{H_{\text{img}}}.
    \end{align}


\section{The model choice}

    For Edge AI, the trade-off between computational speed and
    memory availability is critical. A model must not only deliver
    sufficiently high accuracy, but also remain compact to fit
    onto resource-constrained devices such as microcontrollers.

    YOLO models family \ref{tab:yolo-timeline} presents state-of-the-art
    lightweight versions of model small and efficient enough to be used
    on Edge-devices. They show good results for both - the prediction
    accuracy and inference speed.

    Before YOLO \cite{Redmon2016YOLO} the most common detection approaches used
    two-stage pipelines. The first stage was responsible for generation of
    the box, the other one for classification. It allowed to archive high
    accuracy, but kept these models slow. The YOLO series introduced a single-one
    stage detector - a single convolutional model predicts bounding boxes and
    class probabilities in one pass over the image. It speed up the inference and
    allowed YOLO to compete among Real-Time Inference models.


    \begin{table}[t]
    \centering
    \label{tab:yolo2007_realtime}
    \begin{tabular}{lccc}
    \toprule
    \textbf{Method} & \textbf{Train} & \textbf{mAP} & \textbf{FPS} \\
    \midrule
    \multicolumn{4}{l}{\emph{Real-Time}} \\
    \midrule
    100Hz DPM \cite{sadeghi2014dpm} & 2007 & 16.0 & 100 \\
    30Hz DPM \cite{sadeghi2014dpm}  & 2007 & 26.1 & 30 \\
    Fast YOLO                        & 2007+2012 & 52.7 & \textbf{155} \\
    YOLO                             & 2007+2012 & \textbf{63.4} & 45 \\
    \midrule
    \multicolumn{4}{l}{\emph{Less Than Real-Time}} \\
    \midrule
    Fastest DPM \cite{yan2014fastestdpm}                & 2007 & 30.4 & 15 \\
    R-CNN Minus R \cite{lenc2015rcnnminusr}             & 2007 & 53.5 & 6 \\
    Fast R-CNN \cite{girshick2015fast}                  & 2007+2012 & 70.0 & 0.5 \\
    Faster R-CNN (VGG-16) \cite{ren2015faster}          & 2007+2012 & 73.2 & 7 \\
    Faster R-CNN (ZF) \cite{ren2015faster}              & 2007+2012 & 62.1 & 18 \\
    YOLO VGG-16                                         & 2007+2012 & 66.4 & 21 \\
    \bottomrule
    \end{tabular}
    \caption{Real-Time Systems on PASCAL VOC 2007. Fast YOLO is the fastest
    detector and YOLO attains higher mAP while remaining real-time.
    \cite{Redmon2016YOLO}}
    \end{table}

    Among 11 versions of YOLO models (see Table \cite{khanam2024yolov11}),
    the most important ones are v5, v8, v10 and v11.
    In some sense, v5, introduced by Ultralytics team, created a foundation
    for other versions. It came out in PyTorch implementation, which made it easy
    to deploy, it had multiple sizes (s, m, l, x), so was is easy
    to adjust to the specific task and constraints. It also became possible to
    export model to formats for mobile deployment (as ONNX).
    Version 8 \ref{fig:yolov8_pair} focused more on improving model performance and flexibility.
    Ultralytics improved the backbone. The model adopted anchor-free detection:
    it predicts box coordinates and class probabilities without predefined
    anchor boxes. It simplified the training process and improved
    its generalization. Version 11 released in 2024 by a Tsinghua University group
    introduced an important strategy for the box deduplication. Normally YOLO
    models need an NMS post-process algorithm to remove duplicate detections
    of the same object, but version 10 presented NMS-free detection. It uses a
    dual-label assignment during the training process, so the model predicts only
    one high-confidence box per object. In version 10 large-kernel convolutions and
    partial self-attention also were introduced to increase the receptive field
    without heavy computations. The results were impressive. Yolov10-s model reported
    1.8 times faster inference compared to its competitor - RT-DETR model with similar AP,
    while having 2.8 times fewer parameters (see Table \ref{tab:yolov10_main}).

    \begin{table}[t]
      \centering
      \caption{Comparisons of RT-DETR versus YOLOv8 and YOLOv10 models.
               Latency$^{f}$ denotes the latency of the forward pass
               without post-processing.}
      \label{tab:yolov10_main}
      \resizebox{\textwidth}{!}{%
        \begin{tabular}{lccccc}
          \toprule
          \textbf{Model} & \textbf{\#Param.(M)} & \textbf{FLOPs(G)} &
          \textbf{AP$^{val}$ (\%)} & \textbf{Latency (ms)} & \textbf{Latency$^{f}$ (ms)} \\
          \midrule
          YOLOv8-N \cite{UltralyticsYOLOv8}     & 3.2  &  8.7 & 37.3              & 6.16 & 1.77 \\
          \textbf{YOLOv10-N} \cite{wang2024yolov10} &
          \textbf{2.3} & \textbf{6.7} &
          \textbf{38.5/39.5$^{\dagger}$} &
          \textbf{1.84} & \textbf{1.79} \\
          \midrule
          YOLOv8-S \cite{UltralyticsYOLOv8}  & 11.2 & 28.6 & 44.9  & 7.07 & 2.33 \\
          RT-DETR-R18 \cite{lv2023rtdetr}    & 20.0 & 60.0 & 46.5  & 4.58 & 4.49 \\
          \textbf{YOLOv10-S} \cite{wang2024yolov10} &
          \textbf{7.2} & \textbf{21.6} &
          \textbf{46.3/46.8$^{\dagger}$} &
          \textbf{2.49} & \textbf{2.39} \\
          \bottomrule
        \end{tabular}%
      }
    \end{table}

    The last published version - YOLO11 - didn't introduce significant
    changes in the architecture structure and is mostly based on YOLOv8 design.
    For example, it still used NMS to remove duplicated boxes. Authors
    were focused on optimising the network and achieving higher accuracy per
    parameter. It jumps in accuracy and efficiency over YOLOv8 and shows higher
    mAp with fewer parameters.

    \begin{figure}[!htbp]
      \centering
        \begin{subfigure}{0.48\columnwidth}
        \includegraphics[width=0.6\linewidth]{pics/yolov8_start.pdf}
        \caption{Start of the graph}
        \label{fig:yolov8_start}
      \end{subfigure}
      \begin{subfigure}{0.48\columnwidth}  % or 0.48\textwidth in one-column docs
        \includegraphics[width=0.6\linewidth]{pics/yolov8_end.pdf}
        \caption{End of the graph}
        \label{fig:yolo_end}
      \end{subfigure}\hfill
      \caption{ONNX representation of YOLOv8 layers.}
      \label{fig:yolov8_pair}
    \end{figure}



    \begin{table*}[t]
    \centering
    \caption{Summary of YOLO releases \cite{khanam2024yolov11}}
    \label{tab:yolo-timeline}
    \renewcommand{\arraystretch}{1.15}
    \begin{tabularx}{\textwidth}{@{} l c X X l @{}}
    \toprule
    \textbf{Release} & \textbf{Year} & \textbf{Tasks} & \textbf{Contributions} & \textbf{Framework} \\
    \midrule
    YOLO     & 2015 & Object Detection, Basic Classification
                   & Single-stage object detector
                   & Darknet \\
    YOLOv2   & 2016 & Object Detection, Improved Classification
                   & Multi-scale training, dimension clustering
                   & Darknet \\
    YOLOv3   & 2018 & Object Detection, Multi-scale Detection
                   & SPP block, Darknet-53 backbone
                   & Darknet \\
    YOLOv4   & 2020 & Object Detection, Basic Object Tracking
                   & Mish activation, CSPDarknet-53 backbone
                   & Darknet \\
    YOLOv5  & 2020 & Object Detection, Basic Instance Segmentation (via custom modifications)
                   & Anchor-free detection, SWISH activation, PANet
                   & PyTorch \\
    YOLOv6  & 2022 & Object Detection, Instance Segmentation
                   & Self-attention, anchor-free OD
                   & PyTorch \\
    YOLOv7  & 2022 & Object Detection, Object Tracking, Instance Segmentation
                   & Transformers, E-ELAN reparameterisation
                   & PyTorch \\
    YOLOv8  & 2023 & Object Detection, Instance Segmentation, Panoptic Segmentation, Keypoint Estimation
                   & GANs, anchor-free detection
                   & PyTorch \\
    YOLOv9  & 2024 & Object Detection, Instance Segmentation
                   & PGI and GELAN
                   & PyTorch \\
    YOLOv10 & 2024 & Object Detection
                   & Consistent dual assignments for NMS-free training
                   & PyTorch \\
    YOLOv11 & 2024 & Object Detection; (variants for) Instance Segmentation, Pose, OBB
               & Updated training pipeline and architectures; better accuracy–speed trade-offs; streamlined exports (ONNX/TensorRT) in Ultralytics
               & PyTorch \\
    \bottomrule
    \end{tabularx}
    \end{table*}

    There also exist some other successful approaches in real-time detection. One
    of the earlier mentioned models - RT-DETR - uses a Transformer-based
    architecture. By using transformer decoder, it removes NMS entirely,
    as was done in YOLOv10 too. It allows RT-DETR model performs excellent on GPU
    by efficiently utilizing transformers parallelization. However, YOLO models
    still remain more parameter-efficient and faster on low-power device or CPUs.
    The other well suitable for the Edge detection model is NanoDet. It targets
    fast CPU inference and deployment on edge devices. It can be as small as 0.95
    million parameters, which is smaller than YOLOv8n and Yolov11n models.
    Authours report a quantized INT8 NanoDet model having approximately 97 FPS on
    a mobile CPU. In spite of the speed, the model is notably less accurate, having
    34\% mAP with YOLOv8n having $37\%$ and YOLOv11n - $39.5\%$.

    \textbf{Summary of comparison:} For edge hardware with limited or no GPU, YOLOv11-n or NanoDet would be preferred
    due to their small size (with YOLO offering higher accuracy at a bit
    larger size). With a GPU or NPU, YOLOv11-s or even RT-DETR can be considered.



\section{Hardware}

    Among the most popular development platforms is the Raspberry Pi
    family, which offers a flexible setting for deploying
    lightweight AI systems.
    Raspberry Pi 5 used for this work includes:

    \begin{tabular}{@{}p{0.34\linewidth}p{0.63\linewidth}@{}}
        \multicolumn{2}{l}{\textbf{Compute}}\\
        \midrule
        SoC & 2.4\,GHz quad-core 64-bit Arm Cortex-A76 (crypto ext.); \\
            & 512\,KB L2 per core, \\
            & 2\,MB shared L3 \\
        GPU & VideoCore VII; OpenGL ES 3.1, Vulkan 1.3 \\

        \multicolumn{2}{l}{\textbf{Memory and Storage}}\\
        \midrule
        RAM &  8\, GB \\
        microSD & 64\, GB \\
        \midrule
      \end{tabular}

        An additional hardware used during the experiment is a Hailo-8
        accelerator offering 26 TOPS inferencing performance
        respectively.

    However, the YOLOv8 and YOLOv11 are both provided by \textit{Ultralytics}
    as a PyTorch checkout. In order to use it on accelerator, this checkout has
    to be converted to a suitable format by Hailo Compiler.


\section{Hailo Compiler}

    Hailo compiler \cite{hailo_manual} is needed to load user's models to the Hailo devices.
    The final result of the compiler is a binary file \texttt{.hef}.
    The standard DataFlow for this conversion consists of the three main steps:

    \begin{itemize}
        \item Translation of the source model to Hailo model;
        \item Optimization of model parameters;
        \item Compilation process itself;
    \end{itemize}


    \begin{figure}[H]
      \centering
      \includegraphics[width=0.5\linewidth]{pics/out_graph/hailo_flow.pdf}
      \caption{Build Process \cite{hailo_manual}}
      \label{fig:hailo_flow}
    \end{figure}

    The first step in the Figure \ref{fig:hailo_flow} is a translation from the user's
    model to the \texttt{.har} representation. The Hailo Translation API parses user's model
    and produces a \texttt{.har} (Hailo ARchive).

    \begin{itemize}
        \item HAR is a zip package that contains:
        \begin{itemize}
            \item HN— \enquote{Hailo Network}: a JSON graph of the network (layers,
                tensor shapes/dtypes, connections, metadata).
            \item NPZ—NumPy arrays with the floating-point weights(and sometimes
                other tensors like batch normalization params).
        \end{itemize}
    \end{itemize}

    This step is needed, so the parameters of the model are normalized from the other
    possible checkpoint configurations (used operation and attributes names, FP32 weights),
    and the optimizer has a clean input.

    The second step is model optimization. The key part of the optmization is the conversion
    of the model from FP32 to INT8. It is done with an additional run of calibrarion module.

    There exist different ways to quantize a model. To my knowledge, Hailo Compiler is using
    the Linear Quantization:

    Key benefits from this approach is its efficiency, support by major DL frameworks and easy
    implementation \cite{Han_6.5940_2024}. However, the drawback is a visible precision loss as many nearby
    floating-point values collapse to the same quantized level. Layers with large
    dynamic range (e.g., softmax) are more sensitive to this method.

    Assume an input floating-point value, the linear quantization is
    \[
      q = \operatorname{round}\!\left( Z + \frac{r}{S} \right).
    \]
    \paragraph{Where}
    \begin{itemize}
      \item $r$: original 32-bit floating-point number
      \item $q$: quantized representation (integer)
      \item $S$: scale factor (step size)
      \item $Z$: zero point (integer), which allows $r=0$ to be represented exactly by the integer $Z$
    \end{itemize}

    Dequantization is defined by the formula:
    \[
      r \approx (q - Z)\,S.
    \]

    It is also illustrated at the Figure \ref{fig:quantization}
    \begin{figure}[H]
      \centering
      \includegraphics[width=0.7\linewidth]{pics/out_quant/quantization.pdf}
      \caption{Build Process \cite{Han_6.5940_2024}}
      \label{fig:quantization}
    \end{figure}

    In order to perform this optimization, the calibration set of raw, unlabeled samples is gathered.
    It should match a real input distribution (color space, normalization). The compiler uses this
    set to run a neural network on it and logs per-tensor activation distributions. So, it records
    min/max values and percentiles. By doing so, an optimizer gets $r_{min}$ and $r_{max}$ values.
    Which are used to compute $q_{min}$, $q_{max}$ and $Z$ - point (see Figure \ref{fig:quantization}).
    It is important to do it per tensor as different layers produce values with very different
    ranges/outliers. Choosing ranges from their actual distributions minimizes quantization error.


    The last step is a compilation itself, which the model is compiled to a binary file \texttt{.hef}.
    The compiler is responsible for allocation hardware resources, so the highest possible FPS can be
    archived. It performs it by mapping neural network layers onto the hardware \cite{rptu_eml_ch12_2025} (compute elements,
    memory elements, and control elements) and schedule their execution under the chip's resource
    constraints. If the network is too large, the compiler will partition the network into several
    contexts, allowing bigger models still run with some performance overhead.
    The compiler might tile large tensors, split layers into smaller pieces
    to fit into memory and to utilize parallelizm. Key strategy of this hardware mapping is to perform
    as many computation as possible (possibly for difference layers) on the kept on the chip memory
    tensors. As part of scheduling, compiler also defines the data flow from layer to layer, chaining
    producers and consumers layer clusters. For example, while the layer i is computed on one side of the
    chip, another part of the chip might start executing layer i+1 (Illusration: \ref{fig:conv_reuse}).
    This schedule is constructed during the
    compilation step. Apart from the execution plan, compiler also performs on-chip Non-Maximum
    Suppression (NMS) fusion for models like YOLO. NMS algorithm performs deduplication of several
    predicted for the same object boxes. For instance, the screw could be with different probabilities be
    predicted both as machine and wood screw. So, the post-processing NMS is performed to keep only the most
    suitable box. Hailo's compiler can inregrate NMS as the final layer of the network, so it runs on the
    hailo itself and not on the CPU after the inference. It improves the performance not only due to localization
    of computation, but also due to reduced data transfer - only filtered detections are sent to the
    Raspberry Pi.

    \begin{figure}[H]
      \centering
      \includegraphics[width=0.7\linewidth]{pics/conv_reuse.pdf}
      \caption{Example of the layers reuse: convolutional reuse (Inspired by \cite{emer_sze_chen_2019_dnn_accel_tutorial})}
      \label{fig:conv_reuse}
    \end{figure}

    The final Hailo Executable File contains:
    \begin{itemize}
        \item Low-level program for the target device (layer schedule, the memory buffer addresses/sizes,
            instruction sequences etc.)
        \item Weights and biases
        \item Metadata and calibration information
    \end{itemize}


\section{ONNX (Open Neural Network Exchange)}

ONNX is an open format developed to unify ML models for a variety of
frameworks and compilers by defining a common set of operators and
represent models via graph structure.


\section{Summary of the Literature Study}


% ======= Theory =======
\chapter{Theory}
\section*{Metrics description}
\textbf{Metrics description}:

\begin{itemize}[leftmargin=1.5em]
    \item \textbf{Intersection over Union (IoU)}: \\
            For a predicted box \(p\) and ground-truth box \(g\), the IoU formula is: \\
            \[
                \IoU(p,g) = \frac{|p\cap g|}{|p\cup g|}
            \]
            IoU plays role of evaluating an object localization. \\
            It quantifies the overlap between a prediction and ground truth bounding boxes by calculating
            the ratio of the intersection (overlapping area) to the union (total area covered by both boxes).
            IoU is a building block for calculating mAP. \\
            The resulting score is a value between 0 and 1, which quantifies how accurately a model has located an
            object in an image. A score of 1 represents a perfect match, while a score of 0 indicates no
            overlap at all. \\
            The key part of using IoU is to define \textit{"IoU threshold"}. This threshold is a predefined value
            (e.g., 0.5) that determines whether a prediction is correct. If the IoU score for a predicted box is
            above this threshold, it is classified as a \textit{"true positive"}.
            If the score is below, it's a \textit{"false positive"}. \\
            This threshold directly influences other performance metrics like Precision and Recall. \\

    \item \textbf{Precision and Recall}: \\
            Precision defines the proportion of true positives among all positive predictions,
            measuring the ability to avoid misclassification. \\
            \[
                \Precision = \frac{\TP}{\TP+\FP},
            \]

            On the other hand, Recall calculates the proportion of true positives among all actual
            positives, measuring the model's ability to detect all instances of a class. \\
            \[
                \Recall = \frac{\TP}{\TP+\FN},
            \]

    \item \textbf{Average Precision (AP)}: \\
        AP computes the area under the precision-recall curve \cite{Goodfellow2016}, providing a single value that encapsulates
        the model's precision and recall performance. \\
        \[
            \AP_{\tau} = \int_{0}^{1} p(r;\tau)\,dr,
        \]
        In order to build the precision-recall curve, the score (confidence) threshold (t) is used:
        \begin{itemize}[leftmargin=1.5em]
            \item Every detection has a score $s \in [0,1]$.
            \item We sweep t from high $\to$ low (equivalently sort detections by score desc and add them one-by-one).
            \item At each t we keep detections with $s \ge t $ and recompute P/R.
            \item This sweep produces the P–R curve.
            \item AP is the area under this P–R curve;
        \end{itemize}
        \begin{figure}[H]
          \centering
          \includegraphics[width=0.8\linewidth]{pics/pr_concept.pdf}
          \caption{Precision–Recall curve.}
        \end{figure}



    \item \textbf{Mean Average Precision (mAP)}: \\
        Another metric - mAP,  extends the concept of AP by calculating the average AP values
        across multiple object classes. \\
        This is useful in multi-class object detection scenarios to provide
        a comprehensive evaluation of the model's performance.
        \[
            \mAP_{0.50} = \frac{1}{C}\sum_{c=1}^{C}\AP_{c,\,0.50},
        \]
        \[
            \mAP_{[0.50:0.95]} = \frac{1}{C}\sum_{c=1}^{C}\frac{1}{10}
                                 \sum_{k=0}^{9}\AP_{c,\,(0.50+0.05k)}.
        \]
        The parameter \(C\) is the number of classes for which detection and classification
        are performed. \\
        The subscript \(0.5\), as well as the set [0.50:0.95], denotes the IoU threshold: predictions
        with \(\mathrm{IoU}\ge 0.5\) are counted as true positives (TP). \\

\end{itemize}

% ======= 4 Proposed Solution / Design (Vårt forslag til løsning / design) =======
\chapter{Proposed Solution / Design}
\noindent
% Short intro
\section{Dataset and Annotation }

The dataset consists of 2131 images (see Fig. \ref{fig:roboflow_ex}), which are split into 1983 images for the training,
98 images for the validation and 50 images for the test. Images have a 960x960 size
and are grayscale.

The augmentation includes:

    \begin{itemize}
        \item Outputs per training example: 3
        \item Flip: Horizontal, Vertical
        \item $90^\circ$ Rotate: Clockwise, Counter-Clockwise, Upside Down
        \item Hue: Between $-24^\circ$ and $+24^\circ$
        \item Saturation: Between -25\% and +25\%
        \item Brightness: Between -22\% and +22\%
        \item Exposure: Between -10\% and +10\%
        \item Blur: Up to 2.5px
       \item  Noise: Up to 1.72\% of pixels
    \end{itemize}

    \begin{figure}[H]
      \centering
      \includegraphics[width=0.7\linewidth]{pics/roboflow_annotation_example.png}
      \caption{Roboflow Annotation Illustration}
      \label{fig:roboflow_ex}
    \end{figure}


Annotation was performed with the use of the Roboflow Framework \cite{roboflow_framework}.
It is a platform for computer vision datasets and models. It provides an API for image upload,
fast conversion between formats (e.g., YOLO, COCO), in-browser annotation and labeling for bounding
boxes, segmentation and classification tasks.

The link to the dataset placed at the Roboflow platform is available
in the reference section \cite{embeddedaiproject-id4jh_dataset}

\section{Camera Setup}

    The camera used in the project is IDS uEye UI-3590CP-C-HQ Rev.2.
    It is an industrial machine-vision camera from USB3 uEye Cp series.
    IDS provides a uEye SDK, though which the control of streaming, configuration,
    exposure/gain, etc. is happening.

    The parameters logged while to capturing frames for the further annotation:
        \begin{itemize}
            \item "fps": 7.0,
            \item "exposure_ms": 80,
            \item "width": 4912,
            \item "height": 3684,
            \item "color": true,
            \item "frames": 70,
            \item "frames_saved": 70,
            \item "gain_percent": 10,
            \item "gamma_100": 130,
      \end{itemize}

    \begin{figure}[H]
      \centering
      \includegraphics[width=0.4\linewidth]{pics/camera.png}
      \caption{The picture of the camera is taken from the IDS manual \cite{ids_ui3590cp_ab00606}}
      \label{fig:roboflow_ex}
    \end{figure}




\section{Architecture / Design Details}
% Modules, interfaces, diagrams, constraints, trade-offs.

% ======= 5 Implementation (Vår implementering) =======
\chapter{Implementation}
\noindent
% Short intro
\section{Key Implementation Details}
% Technologies, algorithms, data structures, configurations, noteworthy engineering decisions.

% ======= 6 Testing / Results (Vår uttesting / Resultater) =======
\chapter{Testing and Results}
\noindent
% Short intro
\section{Experimental Setup}
% Test bench, datasets, hardware, software versions, parameters.
\section{Results}
\begin{table}[!t]
\centering
\caption{Validation summaries.}
\label{tab:val_summaries}
\begin{subtable}{\linewidth}
  \centering
  \caption{YOLO8n \cite{yolov8_ultralytics}}
  \begin{tabular}{lrrrrrr}
  \hline
  Class & Images & Instances & Box(P) & R & mAP$_{50}$ & mAP$_{50\mbox{--}95}$ \\
  \hline
  all            & 98 & 363 & 0.927 & 0.951 & 0.964 & 0.733 \\
  machine\_screw & 25 & 123 & 0.931 & 0.967 & 0.989 & 0.625 \\
  miscellaneous  & 14 &  16 & 0.754 & 0.812 & 0.845 & 0.562 \\
  nut            & 16 &  41 & 0.988 & 1.000 & 0.995 & 0.777 \\
  washer         & 20 &  43 & 1.000 & 0.991 & 0.995 & 0.906 \\
  wood\_screw    & 37 & 140 & 0.960 & 0.986 & 0.994 & 0.794 \\
  \hline
  \end{tabular}
  \par\smallskip
  \small Speed per image (GPU): preprocess 4.5\,ms; inference 6.0\,ms; loss 0.0\,ms; postprocess 2.5\,ms.
  \par\smallskip
  \small Speed per image (Raspberry Pi 5): preprocess 8.3\,ms; inference 1018.4\,ms; loss 0.0\,ms; postprocess 1.2\,ms.
  \par\smallskip
  \small Speed per image (Raspberry Pi 5 + Hailo): Frames: 50, time: 18.5s, avg FPS: 2.71
  \par\smallskip
  \small Model summary (fused): 72 layers, 3,006,623 parameters, 0 gradients, 8.1 GFLOPs
\end{subtable}

\medskip

\begin{subtable}{\linewidth}
  \centering
  \caption{YOLO11s}
  \begin{tabular}{lrrrrrr}
  \hline
  Class & Images & Instances & Box(P) & R & mAP$_{50}$ & mAP$_{50\mbox{--}95}$ \\
  \hline
  all            & 98 & 363 & 0.939 & 0.934 & 0.966 & 0.746 \\
  machine\_screw & 25 & 123 & 0.877 & 0.983 & 0.978 & 0.649 \\
  miscellaneous  & 14 &  16 & 0.899 & 0.688 & 0.867 & 0.565 \\
  nut            & 16 &  41 & 0.970 & 1.000 & 0.995 & 0.805 \\
  washer         & 20 &  43 & 0.973 & 1.000 & 0.995 & 0.909 \\
  wood\_screw    & 37 & 140 & 0.978 & 1.000 & 0.995 & 0.802 \\
  \hline
  \end{tabular}
  \par\smallskip
  \small Speed per image (GPU): preprocess 4.5\,ms; inference 9.8\,ms; loss 0.0\,ms; postprocess 3.7\,ms.
  \par\smallskip
  \small YOLO11s summary (fused): 100 layers, 9,414,735 parameters, 0 gradients, 21.3 GFLOPs
\end{subtable}

\medskip

\begin{subtable}{\linewidth}
  \centering
  \caption{YOLO11n \cite{yolo11_ultralytics}}
  \begin{tabular}{lrrrrrr}
  \hline
  Class & Images & Instances & Box(P) & R & mAP$_{50}$ & mAP$_{50\mbox{--}95}$ \\
  \hline
  all            & 98 & 363 & 0.921 & 0.935 & 0.964 & 0.739 \\
  machine\_screw & 25 & 123 & 0.844 & 0.935 & 0.957 & 0.611 \\
  miscellaneous  & 14 &  16 & 0.922 & 0.741 & 0.888 & 0.608 \\
  nut            & 16 &  41 & 0.970 & 1.000 & 0.995 & 0.799 \\
  washer         & 20 &  43 & 0.996 & 1.000 & 0.995 & 0.902 \\
  wood\_screw    & 37 & 140 & 0.873 & 1.000 & 0.983 & 0.773 \\
  \hline
  \end{tabular}
  \par\smallskip
  \small Speed per image (GPU): preprocess 0.5\,ms; inference 5.6\,ms; loss 0.0\,ms; postprocess 0.9\,ms.
  \par\smallskip
  \small 100 layers, 2,583,127 parameters, 0 gradients, 6.3 GFLOPs.
\end{subtable}

\end{table}

\begin{table}[tb]
\centering
\caption{YOLO \textit{nano} (n) variants on COCO at 640$\times$640.}
\label{tab:yolo_n_variants}
\begin{tabular}{lccc}
\hline
Model & Params (M) & FLOPs (B) & mAP$_{50\text{--}95}$ (val) \\
\hline
YOLOv5n & 1.9 & 4.5  & 28.0 \\
YOLOv8n & 3.2 & 8.7  & 37.3 \\
YOLO11n & 2.6 & 6.5  & 39.5 \\
\hline
\end{tabular}

\medskip
\footnotesize{Numbers from Ultralytics model cards: v5 \cite{UltralyticsYOLOv5}, v8 \cite{UltralyticsYOLOv8}, v11 \cite{UltralyticsYOLO11Repo}.}
\end{table}

% --- YOLO s-variants (COCO, 640x640) ---
\begin{table}[tb]
\centering
\caption{YOLO \textit{small} (s) variants on COCO at 640$\times$640.}
\label{tab:yolo_s_variants}
\begin{tabular}{lccc}
\hline
Model & Params (M) & FLOPs (B) & mAP$_{50\text{--}95}$ (val) \\
\hline
YOLOv5s & 7.2  & 16.5 & 37.4 \\
YOLOv8s & 11.2 & 28.6 & 44.9 \\
YOLO11s & 9.4  & 21.5 & 47.0 \\
\hline
\end{tabular}

\medskip
\footnotesize{Numbers from Ultralytics model cards: v5 \cite{UltralyticsYOLOv5}, v8 \cite{UltralyticsYOLOv8}, v11 \cite{UltralyticsYOLO11Repo}.}
\end{table}



\begin{table}[tb]
  \centering
  \caption{Hailo benchmark results for \texttt{yolov8\_custom}.}
  \label{tab:hailo-bench}
  \begin{tabular}{lll}
    \toprule
    Metric & Mode       & Value \\
    \midrule
    FPS    & HW-only    & 142.371 \\
    FPS    & Streaming  & 142.370 \\
    Latency & HW        & 10.796 ms \\
    \bottomrule
  \end{tabular}
\end{table}


\begin{table}[tb]
  \centering
  \caption{HEF metadata (\texttt{yolov8\_custom.hef}).}
  \label{tab:hef-meta}
  \begin{tabular}{ll}
    \toprule
    Field & Value \\
    \midrule
    Architecture & HAILO8 \\
    Network group & \texttt{yolov8\_custom} (Single Context) \\
    Network & \texttt{yolov8\_custom/yolov8\_custom} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[tb]
  \centering
  \caption{VStream infos.}
  \label{tab:vstream}
  \begin{tabular}{lll}
    \toprule
    Direction & Name & Spec \\
    \midrule
    Input  & \texttt{yolov8\_custom/input\_layer1} & \texttt{UINT8, NHWC(960$\times$960$\times$3)} \\
    Output & \texttt{yolov8\_custom/yolov8\_nms\_postprocess} & \texttt{FLOAT32, HAILO NMS BY CLASS} \\[-2pt]
           &                                         & \small(number of classes: 5, max bboxes/class: 100, max frame size: 10020) \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[tb]
  \centering
  \caption{Post-process (\texttt{YOLOV8-Post-Process}) parameters.}
  \label{tab:nms-params}
  \begin{tabular}{ll}
    \toprule
    Parameter & Value \\
    \midrule
    Op / Name & \texttt{YOLOV8 / YOLOV8-Post-Process} \\
    Score threshold & 0.001 \\
    IoU threshold & 0.70 \\
    Classes & 5 \\
    Cross classes & \texttt{false} \\
    NMS results order & \texttt{BY\_CLASS} \\
    Max bboxes per class & 100 \\
    Image size & 960 $\times$ 960 \\
    \bottomrule
  \end{tabular}
\end{table}




% Tables, figures; explain what the numbers mean.

% ======= 7 Discussion (Diskusjon) =======
\chapter{Discussion}
% How to interpret results? Do they match expectations/specifications (from Functional Specification)?
% Causes of deviations? What could be done differently? What should be added?
% This leads into Future Work.
\section{Discussion Points}
% by theme (performance, robustness, cost, etc.)

% ======= 8 Conclusion (Konklusjon) =======
\chapter{Conclusion}
% Concise wrap-up: problem, approach, key results, contributions, significance.

% ======= 9 Future Work (Videre arbeid) =======
\chapter{Future Work}
% Bullet-point summary tied to Discussion.
\begin{itemize}[leftmargin=1.5em]
    \item Item one
    \item Item two
    \item Item three
\end{itemize}

% ======= References (Referanseliste) =======

\bibliographystyle{ieeetr}  % or plain, unsrt, abbrv, IEEEtran, etc.
\bibliography{refs}

\cleardoublepage

% ======= Appendices (Vedlegg A/B) =======
\appendix

\chapter{Text (Appendix A)}
% Additional materials, extended tables/figures, code listings if allowed.

\chapter{Text (Appendix B)}
% Additional materials.

\end{document}
